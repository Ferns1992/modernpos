services:
  modern-pos:
    build: .
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/data/pos.db
    volumes:
      - pos-data:/data
    restart: unless-stopped

volumes:
  pos-data: