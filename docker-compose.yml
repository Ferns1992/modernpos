version: "3.8"

services:
  modern-pos:
    build: .
    image: modern-pos:latest
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/data/pos.db
    volumes:
      - pos-data:/data
    restart: unless-stopped

volumes:
  pos-data: