services:
  pos-app:
    image: pos-app:latest
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./pos.db:/data/pos.db
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/data/pos.db
    restart: always
