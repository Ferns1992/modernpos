services:
  pos-app:
    image: pos-app:latest
    build: .
    ports:
      - target: 4000
        published: 4000
        protocol: tcp
        mode: ingress
    volumes:
      - /data/pos.db:/data/pos.db
    environment:
      - NODE_ENV=production
    deploy:
      restart_policy:
        condition: on-failure
      placement:
        constraints: [node.role == manager]
