version: '3'
services:
  modern-pos:
    image: modern-pos:latest
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./data:/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/data/pos.db
    restart: always
