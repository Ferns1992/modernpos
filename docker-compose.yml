services:
  pos-app:
    image: pos-app:latest
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./data:/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/data/pos.db
    restart: always
